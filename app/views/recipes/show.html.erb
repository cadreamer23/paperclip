<p id="notice"><%= notice %></p>

<%= image_tag @recipe.preview.url %>
<%= image_tag @recipe.preview.url(:medium) %>
<%= image_tag @recipe.preview.url(:thumb) %>

<p>
  <strong>Title:</strong>
  <%= @recipe.title %>
</p>

<p>
  <strong>Instructions:</strong>
  <%= @recipe.instructions %>
</p>

<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>
